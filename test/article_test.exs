defmodule ArticleTest do
  use ExUnit.Case

  # test "article parser works" do
  #   html = sample_website "venturebeat"
  #   snippets = Scrape.Util.HTML.to_fulltext(html)
  #   assert snippets == false
  # end
  #
  # defp sample_website(name) do
  #   File.read! "test/sample_data/#{name}-website.html.eex"
  # end
end
